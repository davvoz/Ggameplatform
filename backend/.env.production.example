# Backend Environment Variables - PRODUCTION
# IMPORTANT: Copy this to .env and fill in real values!

# Allowed CORS Origins (comma-separated)
# Replace with your actual production domain(s)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Admin API Key - GENERATE RANDOM!
# Command: python -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_API_KEY=REPLACE_WITH_RANDOM_KEY_FROM_COMMAND_ABOVE

# JWT Secret Key - GENERATE RANDOM!
# Command: python -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_SECRET_KEY=REPLACE_WITH_RANDOM_KEY_FROM_COMMAND_ABOVE
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60

# Database Configuration
# DATABASE_URL=postgresql://user:password@host:port/dbname

# STEEM Configuration
STEEM_ACCOUNT=cur8.fund
STEEM_ACTIVE_KEY=YOUR_ACTIVE_PRIVATE_KEY_HERE
STEEM_POSTING_KEY=YOUR_POSTING_PRIVATE_KEY_HERE

# Server Configuration
HOST=0.0.0.0
PORT=8000

# SECURITY CHECKLIST:
# ✅ 1. NEVER commit .env file to git!
# ✅ 2. Use strong random keys (see commands above)
# ✅ 3. Change ALLOWED_ORIGINS to your production domain
# ⚠️ 4. Enable HTTPS in production (use reverse proxy like nginx)
# ⚠️ 5. Rotate keys periodically (every 90 days recommended)
