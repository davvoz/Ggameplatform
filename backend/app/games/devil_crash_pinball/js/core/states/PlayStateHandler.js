import { LiveStateHandler } from './LiveStateHandler.js';

/**
 * Handles PLAY: pure live-physics loop — no additional input handling beyond
 * what LiveStateHandler already provides.
 */
export class PlayStateHandler extends LiveStateHandler {}
